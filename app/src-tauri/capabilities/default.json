{
  "$schema": "gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs, native folder picker, updater + relaunch. File writes go through the app's own write_backup/prune_backups commands.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "updater:default",
    "process:allow-restart"
  ]
}
